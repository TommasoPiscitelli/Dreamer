[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dreamer-carracing"
version = "0.1.0"
description = "Dreamer-style latent imagination for CarRacing"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
